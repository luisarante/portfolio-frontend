import { Link } from "react-router-dom";

export function Footer() {
    return(
        <footer className="bg-surface text-white py-6 mt-12 relative">
            <a href="#hero">Voltar ao topo</a>
        </footer>
    )
}